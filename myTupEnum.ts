//this allows string or number to be the value
const user:(string|number)[]=["aa"];

//to keep the datatype in a particular sequence.
let tuser:[string,number,boolean]
tuser=["ameya",1,true]
type User=[string,number,boolean]
let var1:User=["ameya",1,true]
console.log(var1);

//
var1.push(false);

export {}
