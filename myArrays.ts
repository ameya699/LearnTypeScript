//ways of defining the type

const superHeros:string[]=[]
const heroPowers:number[]=[]
const herrpw:Array<number>=[]


type User = {
    name:string,
    isActive:boolean
}

const allUsers:User[]=[]

allUsers.push({name:"ameya",isActive:true});

console.log(allUsers);